# Illithid Task Context

The task dashboard is `_work/tasks.csv`. Add expanded context here only when a
task cannot remain independently understandable in its dashboard row or owning
plan.

Production discovery tasks are read-only by default. A task that changes the
live service must separately identify authorization, backup and rollback
preconditions, private-evidence handling, and the responsible `site-ops`
coordination record.
